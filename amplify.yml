version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd voicenote-ai
        - rm -rf node_modules package-lock.json
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: voicenote-ai/dist
    files:
      - '**/*'