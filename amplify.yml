version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd voicenote-ai
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: voicenote-ai/dist
    files:
      - '**/*'