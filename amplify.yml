version: 1

frontend:
  phases:
    preBuild:
      commands:
        - cd voicenote-ai
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: voicenote-ai/dist
    files:
      - '**/*'
  cache:
    paths:
      - voicenote-ai/node_modules/**/*